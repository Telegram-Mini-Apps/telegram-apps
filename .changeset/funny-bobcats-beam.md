---
"@telegram-apps/bridge": patch
---

Disallow using `bg_color` and `secondary_bg_color` when calling `web_app_set_background_color`. Do the same with `web_app_set_bottom_bar_color` and also disallow `bottom_bar_bg_color` value.
