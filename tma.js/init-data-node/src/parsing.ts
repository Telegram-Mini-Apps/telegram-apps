import { parseInitDataQuery, parseInitDataQueryFp } from '@tma.js/transformers';

/**
 * Parses an incoming value as init data.
 */
export const parse = parseInitDataQuery;

/**
 * Parses an incoming value as init data.
 */
export const parseFp = parseInitDataQueryFp;
