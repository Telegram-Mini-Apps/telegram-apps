import { createSanitizer } from '~/helpers/createSanitizer.js';

/**
 * Removes specified properties.
 * @param props - properties.
 * @param keys - keys to remove.
 */
export const sanitizeProps = createSanitizer([]);
