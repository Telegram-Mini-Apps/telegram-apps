import React from 'react';
import {Switch} from '../../src/components/Switch';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <Switch platform={'ios'} activeColor={'#56ACE9'}/>,
  document.getElementById('root')!,
);