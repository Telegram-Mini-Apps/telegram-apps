import {SwitchBase} from '../base';

import withStyles from '../../../styles/customStyles';
import styles from './styles.scss';

/**
 * Switch used on iOS platform.
 * @param props
 * @constructor
 */

export const IOSSwitch = withStyles(styles, SwitchBase);