/**
 * Property name in window which determines if global receiveEvent function
 * is installed.
 */
export const WindowReceiverDefined = '__receiverDefined';

/**
 * Property name in window which contains global event emitter instance.
 */
export const WindowGlobalEventEmitter = '__globalEventEmitter';